# Lista de compras com sincronismo em nuvem

Este é um projeto funcional para aperfeiçoar os conhecimentos em  Kotlin, Arquitetura MVVM e SOLID.

Kotlin é usada como linguagem principal do projeto, para tarefas em segundo plano, Coroutines, a persistência de dados local é feita com Room (DAO)  a arquitetura escolhida para o projeto foi a MVVM junto com componentes do Android Jetpack 

A parte do sincronismo na nuvem ainda não foi implementada, mas será feita com o Firebase.

Por vir:
Login com Google
Sincronismo de dados na nuvem + em tempo real
